Ces commandes sont à effectuer dans ``loveletter``.

Vous pouvez compiler avec la commande :
```
./compile.sh
```

Executer avec :
```
./run.sh
```

Et clean le projet avec (supprimer les .class):
```
./clean.sh
```

